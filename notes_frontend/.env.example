# Copy this file to .env and fill the values before running the app.
# These are the only environment variables this app reads.

# Supabase project URL (Settings -> API -> Project URL)
REACT_APP_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co

# Supabase anon/public API key (Settings -> API -> anon public)
REACT_APP_SUPABASE_KEY=YOUR-ANON-PUBLIC-KEY
